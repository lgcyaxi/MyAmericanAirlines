name: my_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=2.1.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0
  location: ^2.3.5
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.7.3"
  pedantic: ^1.8.0
  test: ^1.6.0

flutter_icons:
  android: true
  ios: true
  image_path: "images/aa.png"

flutter:
  uses-material-design: true
  assets:
    - images/T1.png
    - images/aa_logo.png

